# 部署与使用文档（模块7）

## 1. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（本地演示）；Linux 可运行（路径差异见 §6） |
| Python | 3.10 ~ 3.12（开发环境 3.11.10） |
| 浏览器 | Chrome / Edge（现代浏览器即可） |

## 2. 一键安装（首次）

```bat
cd D:\ML_help
setup.bat
```

脚本会：创建 `.venv` 虚拟环境 → 升级 pip → 安装 `requirements.txt` 全部依赖。

主要依赖（已在 requirements.txt 中锁定版本）：
- fastapi 0.115.6 / uvicorn
- pandas 2.2.3 / scikit-learn 1.5.2 / matplotlib 3.9.2
- python-multipart（文件上传）、httpx（测试用）

> 注意：本项目**未使用**任何成熟 AutoML 库（不依赖 pycaret 等），
> 全部模型训练逻辑为手写 sklearn 代码，方便毕设论文中阐述原理。

## 3. 启动服务

```bat
start.bat
```

或手动执行：

```bat
cd D:\ML_help
.venv\Scripts\python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

浏览器访问 **http://127.0.0.1:8000**

## 4. 使用流程

1. **上传数据**：支持 CSV / Excel（xlsx、xls），≤50MB。
   自动识别 utf-8 / gbk / gb18030 编码；页面上可预览前 5 行、查看缺失统计。
2. **选择标签**：选择目标列（自动推荐候选标签）、任务类型（分类/回归/自动）、
   勾选需剔除的 ID 列（已自动识别"学号/编号"类列并默认勾选）。
3. **自动训练**：数据探查 → 预处理（缺失填充、类别编码、标准化）→
   多模型对比（逻辑回归/随机森林/决策树/SVM，或线性回归/Ridge/决策树/随机森林）→
   评估（Accuracy/F1/AUC 或 R²/MAE）→ 绘图（混淆矩阵、特征重要性、散点图）。
4. **查看结果**：模型对比表（最优模型高亮）、图表、特征重要性 Top15、
   可下载 Markdown 实验报告、可复现 Python 脚本、结果 JSON。
5. **（可选）AI 解读**：结果页自动调用 QClaw Agent 生成解读与调参建议；
   Agent 未连接时自动降级为本地规则解读，不影响主流程。

## 5. 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| POST | `/api/upload` | 上传数据，返回 file_id |
| GET | `/api/explore?file_id=` | 数据概览（样本/缺失/列类型/预览/推荐标签） |
| POST | `/api/train` | 提交训练任务（file_id + target_col + task_type + id_cols） |
| GET | `/api/download/{task_id}/result.json` | 下载结果 JSON |
| GET | `/api/download/{task_id}/report.md` | 下载 Markdown 实验报告 |
| GET | `/api/download/{task_id}/script.py` | 下载复现脚本 |
| GET | `/api/ai/interpret/{task_id}` | AI 解读（可选增强） |

## 6. Linux / macOS 部署

```bash
cd ML_help
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

- 上传的文件保存在 `outputs/uploads/`，训练结果保存在 `outputs/{task_id}/`，
  均为运行时自动创建，无需手动建目录。
- 如需远程访问，将 `--host` 改为 `0.0.0.0`，并注意防火墙放行端口。

## 7. 目录结构

```
ML_help/
├── app/
│   ├── main.py              # FastAPI 入口，路由注册
│   ├── config.py            # 配置（存储路径、编码列表、7:3 划分）
│   ├── routers/
│   │   ├── upload.py        # POST /api/upload
│   │   ├── explore.py       # GET /api/explore
│   │   ├── train.py         # POST /api/train
│   │   ├── download.py      # GET /api/download/{task_id}/{artifact}
│   │   └── ai.py            # GET /api/ai/interpret/{task_id}
│   ├── ml/
│   │   ├── explore.py       # 数据读取、列类型推断、ID检测、标签推荐
│   │   ├── preprocess.py    # 缺失填充、编码、标准化、任务类型判断
│   │   ├── train.py         # 多模型训练流水线（核心）
│   │   ├── evaluate.py      # 分类/回归评估指标
│   │   ├── plots.py         # matplotlib 图表
│   │   ├── report.py        # Markdown 报告 + 复现脚本生成
│   │   └── ai_advisor.py    # QClaw Agent 解读（可降级）
│   └── static/
│       ├── index.html       # 上传页
│       ├── select.html      # 标签选择页
│       ├── result.html      # 结果展示页
│       ├── css/style.css
│       └── js/upload.js, select.js, result.js
├── sample_data/             # 示例数据集（学生成绩）
├── tests/                   # pytest 单测 + 端到端测试脚本
├── outputs/                 # 运行时产物（自动生成）
├── requirements.txt
├── setup.bat / start.bat    # Windows 一键脚本
└── 部署文档.md
```

## 8. 常见问题

- **上传 422 错误**：文件不是 csv/xlsx/xls，或超过 50MB。
- **乱码**：CSV 非 UTF-8 时已自动尝试 GBK/GB18030；若仍乱码，请将文件另存为 UTF-8。
- **训练报错**：目标列全为缺失值，或样本数过少（<10）。
- **端口被占用**：修改 start.bat 中 `--port` 参数。
- **复现脚本**：下载的 `script.py` 中 `DATA_PATH` 已指向原始数据文件，
  复制到任何装有 pandas/sklearn/matplotlib 的环境即可运行。
